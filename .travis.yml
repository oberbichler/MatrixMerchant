sudo: false
language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

before_script:
  - wget http://bitbucket.org/eigen/eigen/get/3.3.4.tar.bz2 -O - | tar -xj -C $HOME

script:
  - mkdir build
  - cd build
  - cmake .. -DEIGEN3_ROOT="$HOME/eigen-eigen-5a0156e40feb"
  - cmake --build .
  - ./test/run_tests
